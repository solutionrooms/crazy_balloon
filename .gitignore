# Original copyrighted Taito ROM binaries — required locally for tools/ only,
# NEVER published. The game's runtime assets are baked into src/gfx + src/levels.
rom/

# Reference material: copyrighted video frames + large regenerable ROM-data
# renders. Keep only the written notes.
reference/*
!reference/NOTES.md

# Node / Vite
node_modules/
dist/
*.log
.DS_Store

# Local editor / tooling state
.claude/
__pycache__/
*.pyc
